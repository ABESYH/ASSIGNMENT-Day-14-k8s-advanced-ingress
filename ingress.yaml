apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: abe-assignment-ingress
spec:
  ingressClassName: traefik
  rules:
  - host: abe.assignment.apps.k3s.zauqilab.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: abe-assignment-service
            port:
              number: 80
